{
  "name": "pc-builder-ai",
  "version": "1.0.0",
  "description": "PC Builder AI - Complete PC building assistant",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "echo 'Building application...' && cd frontend && NODE_ENV=production yarn build && echo 'Build complete!'",
    "dev": "node app.js",
    "postinstall": "echo 'Installing dependencies...' && cd frontend && yarn install --production && cd ../backend && pip install -r requirements.txt && echo 'Dependencies installed!'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "cors": "^2.8.5",
    "helmet": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "author": "PC Builder AI",
  "license": "MIT",
  "keywords": ["pc", "builder", "ai", "computer", "hardware"]
}