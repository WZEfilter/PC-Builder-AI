{
  "name": "pc-builder-ai",
  "version": "1.0.0",
  "description": "PC Builder AI - Complete PC building assistant",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Building frontend...' && cd frontend && NODE_ENV=production yarn build && echo 'Frontend build complete!'",
    "prebuild": "echo 'Preparing build environment...'",
    "postinstall": "echo 'Installing dependencies...' && cd frontend && yarn install --frozen-lockfile && echo 'Frontend dependencies installed!' && cd ../backend && pip install -r requirements.txt && echo 'Backend dependencies installed!'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "PC Builder AI",
  "license": "MIT"
}